import javafx.scene.layout.Pane;

public class FxmlLoader {
    private Pane view;

    public Pane getPage(String pageName) {
        try {
            URL fileURL = ViewController.class.getResource(/);
        } catch (Exception E) {
            System.out.println("No page " + fileName + " please check FxmlLoader.");
        }
    }
}
